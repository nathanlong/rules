---
layout: layouts/home.njk
---

<p>Rules on the go for some of my favorite games, along with modifications and house rules.</p>

{% set maxPosts = collections.posts.length | min(3) %}
<h1>Latest {% if maxPosts == 1 %}Game{% else %}{{ maxPosts }} Games{% endif %}</h1>

{% set postslist = collections.posts | head(-3) %}
{% set postslistCounter = collections.posts | length %}
{% include "postslist.njk" %}

<p>More games can be found in <a href="{{ '/games/' | url }}">the full list</a>.</p>
